# Build artifacts
build/
cmake-build-*/
*.o
*.a
*.so
*.so.*
bench-out/

# Benchmark-generated packs (large, regenerable)
bench/*.pack

# Editors
.vscode/
.idea/
*.swp
# ASan/UBSan, TSan and debug build dirs
build-asan/
build-dbg/
build-tsan/

# M4-2: 下载的 vanilla 服务端 jar + ASM 库 + 生成的 world pack(可再生)
mc-server/
lib/

# M5 Task 1: Provider 测试用的两代真实 jar(可再生)
lib/mc-versions/

# 26.2 jar 引导时 Bootstrap.wrapStreams 写出的服务端日志(每次运行可再生)
logs/
