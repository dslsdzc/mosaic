#ifndef MOSAIC_INTERNAL_H
#define MOSAIC_INTERNAL_H
/* mosaic_internal.h — 内部公共头,后续任务逐步补充(runtime 状态、表结构等)。
   Task 1 建立的最小占位:让 src/*.c 占位文件可编译。 */
#include "mosaic/base.h"
#include "mosaic/pack.h"
#endif
